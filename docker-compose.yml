services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
    volumes:
      - quizz-data:/data
      # Avatars live on the host — drop image files here and they appear in the picker
      - /opt/quizz/avatars:/data/avatars

volumes:
  quizz-data:
